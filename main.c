#include <stdio.h> //헤더파일 std스탠다드 io입출력
#include <stdlib.h>

int main()//괄호는 함수이기에 값을 넣기위한 공간 비어있기에 외부로부터 값을 안받음 main함수는 값이 없어도 자동실행 int=정수(데이터타입) main함수는 예외적으로 자동실행
{
    printf("Hello world!\n");//괄호안의 문자를 출력 문자를 나열=문자열 charactor=문자 string=문자열 \n=nextline 줄바꿈
    return 0;//굵은글씨들은 미리 정해놓은 키워드 (함수를 호출한다=함수를 실행한다)
}
